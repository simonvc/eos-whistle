#!/bin/sh
# Placeholder script to demonstrate how a firmware image could be flashed
# Adjust the command below to match your toolchain setup

echo "Flashing firmware..."
# qfprog --port /dev/ttyACM0 --bitstream build/firmware.bin
